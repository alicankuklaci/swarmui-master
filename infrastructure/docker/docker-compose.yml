version: '3.8'

services:
  mongodb:
    image: mongo:4.4
    container_name: swarmui-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: swarmui
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
      - ./mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
    networks:
      - swarmui-network

  redis:
    image: redis:7.2-alpine
    container_name: swarmui-redis
    restart: unless-stopped
    command: redis-server --requirepass redispassword
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - swarmui-network

volumes:
  mongodb_data:
  redis_data:

networks:
  swarmui-network:
    driver: bridge
