SwarmUI projesinde Phase 6'yi implemente edeceksin (Polish & Production).
Proje: /home/nlc/projects/swarmui
PLAN.md dosyasini oku ve takip et.

Mevcut durum: Phase 0-5 tamamlandi.

GOREV - PHASE 6: Polish & Production

1. E2E test suite (Playwright):
   - apps/frontend/tests/e2e/login.spec.ts (login -> dashboard)
   - containers page load test
   - basic navigation test
   - pnpm test:e2e script'i

2. Unit testler:
   - backend: auth.service, users.service icin temel unit testler
   - coverage hedefi: kritik moduller

3. Performance optimizasyonu:
   - MongoDB indexler: users(email, username), activity_logs(timestamp), auth_logs(timestamp), gitops_deployments(endpointId), backups(nextRunAt)
   - query optimizasyonu (list endpoints sayfalama)

4. Rate limiting + DDoS:
   - @nestjs/throttler kurulumu
   - login endpoint'lerine rate limit (10 req/min)

5. Security hardening:
   - Helmet aktif
   - CORS whitelist
   - CSRF koruma (API için token bazli)

6. Monitoring:
   - /health endpoint (database, redis check)
   - Prometheus metrics (@willsoto/nestjs-prometheus)

7. Dokumantasyon:
   - docs/deployment/README.md: production deploy (Docker Swarm)
   - docs/api/README.md: OpenAPI & endpoints

8. CI/CD (GitHub Actions):
   - lint, test, build pipeline

9. Build / production config:
   - Dockerfile update
   - .env.prod örneği

Tum isler bitince:
cd /home/nlc/projects/swarmui && git add -A && git commit -m "chore: Phase 6 polish & production"

Sonra bildir:
openclaw system event --text "SwarmUI Phase 6 tamamlandi! Production hazir." --mode now
